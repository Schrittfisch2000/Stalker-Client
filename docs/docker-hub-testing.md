# Docker Hub Testablauf

Beim ersten Release wird geprüft:

- Image ist auf Docker Hub vorhanden
- amd64 Pull funktioniert
- arm64 Pull funktioniert
- UGREEN kann das Image starten
- Konfiguration bleibt über Volume erhalten

Die lokale Build-Variante bleibt für Entwickler weiterhin möglich.
