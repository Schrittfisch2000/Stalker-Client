# Docker Hub Betrieb

Nach einem erfolgreichen Release wird das Image automatisch veröffentlicht.

Die UGREEN-Instanz verwendet danach nur noch ein Redeploy, um das aktuelle Image zu laden.

Die Konfiguration bleibt getrennt vom Container bestehen.
